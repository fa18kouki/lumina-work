-- =============================================
-- RUN-508: Offer に面接候補日時カラムを追加
--
-- 店舗がオファー作成時に面接候補日時を 3 件登録 → キャストが承諾時に
-- そのうち 1 件を選んで Interview を即作成する、というフローのための
-- データ受け皿。
--
-- 完全に additive:
--   - 既存テーブルに新規カラムを 1 本追加するだけ
--   - DEFAULT '{}' (空配列) なので既存行は影響を受けない
--   - NOT NULL 制約は付けるが DEFAULT があるため既存行も埋まる
-- =============================================

ALTER TABLE "offers"
    ADD COLUMN "interview_slots" TIMESTAMP(3)[] NOT NULL DEFAULT ARRAY[]::TIMESTAMP(3)[];
